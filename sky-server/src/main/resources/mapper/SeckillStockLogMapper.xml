<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SeckillStockLogMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into seckill_stock_log (activity_id,user_id,quantity,before_stock,after_stock,status, create_time)
        values (#{activityId}, #{userId}, #{quantity}, #{beforeStock}, #{afterStock}, #{status}, #{createTime})
    </insert>
    <select id="getByActivityId" resultType="com.sky.entity.SeckillStockLog">
        select *
        from seckill_stock_log
        where activity_id = #{activityId}
        order by create_time desc
    </select>
    <select id="getByUserId" resultType="com.sky.entity.SeckillStockLog">
        select *
        from seckill_stock_log
        where user_id = #{userId}
        order by create_time desc
    </select>
</mapper>
