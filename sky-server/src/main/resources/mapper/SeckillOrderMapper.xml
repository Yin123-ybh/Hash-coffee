<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SeckillOrderMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into seckill_order (activity_id, user_id,dish_id,quantity,seckill_price,total_amount,status, create_time,update_time)
        values (#{activityId}, #{userId}, #{dishId},#{quantity},#{seckillPrice},#{totalAmount},#{status}, #{createTime},#{updateTime})
    </insert>
    <update id="updateStatus">
        update seckill_order
        set status = #{status},
            update_time = now()
        where id = #{id};
    </update>
    <select id="getById" resultType="com.sky.entity.SeckillOrder">
        select *
        from seckill_order
        where id = #{id};
    </select>
    <select id="getByUserId" resultType="com.sky.entity.SeckillOrder">
        select *
        from seckill_order
        where user_id = #{userId}
        order by create_time desc
    </select>
    <select id="getByActivityId" resultType="com.sky.entity.SeckillOrder">
        select *
        from seckill_order
        where activity_id = #{activityId}
        order by create_time desc
    </select>
</mapper>
