<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CouponTemplateMapper">

    <select id="pageQuery" resultType="com.sky.entity.CouponTemplate">
        select * from coupon_template
        <where>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="getById" resultType="com.sky.entity.CouponTemplate">
        select * from coupon_template where id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into coupon_template
        (name, type, discount_type, discount_value, min_amount, max_discount, total_count, used_count, per_user_limit, 
         valid_days, start_time, end_time, status, create_time, update_time, create_user, update_user)
        values
        (#{name}, #{type}, #{discountType}, #{discountValue}, #{minAmount}, #{maxDiscount}, #{totalCount}, #{usedCount}, 
         #{perUserLimit}, #{validDays}, #{startTime}, #{endTime}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="update">
        update coupon_template
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="discountType != null">discount_type = #{discountType},</if>
            <if test="discountValue != null">discount_value = #{discountValue},</if>
            <if test="minAmount != null">min_amount = #{minAmount},</if>
            <if test="maxDiscount != null">max_discount = #{maxDiscount},</if>
            <if test="totalCount != null">total_count = #{totalCount},</if>
            <if test="usedCount != null">used_count = #{usedCount},</if>
            <if test="perUserLimit != null">per_user_limit = #{perUserLimit},</if>
            <if test="validDays != null">valid_days = #{validDays},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from coupon_template where id = #{id}
    </delete>

    <update id="updateStatus">
        update coupon_template set status = #{status}, update_time = now(), update_user = #{updateUser} where id = #{id}
    </update>

    <select id="getStatistics" resultType="com.sky.vo.CouponStatisticsVO">
        select 
            count(c.id) as totalCoupons,
            count(case when c.status = 1 then 1 end) as usedCoupons,
            count(case when c.status = 2 then 1 end) as expiredCoupons,
            coalesce(sum(oc.discount_amount), 0) as totalDiscount
        from coupon c
        left join order_coupon oc on c.id = oc.coupon_id
        left join coupon_template ct on c.template_id = ct.id
        where c.create_time between #{startTime} and #{endTime}
    </select>

    <select id="listEnabled" resultType="com.sky.entity.CouponTemplate">
        select * from coupon_template where status = 1 and start_time &lt;= now() and end_time &gt;= now()
    </select>

</mapper>
